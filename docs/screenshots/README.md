# Screenshots

Drop screenshots here with the exact filenames below — the main [README.md](../../README.md) already links to these paths, so they'll appear automatically once added.

| Filename | Page | Suggested state |
|---|---|---|
| `home.png` | Marketing home (`/`) | Light mode, full hero visible |
| `about.png` | About (`/about`) | Scrolled to the DevHatch Labs section |
| `pricing.png` | Pricing (`/pricing`) | All three tiers visible |
| `login.png` | Login (`/login`) | Empty form |
| `dashboard.png` | Dashboard overview (`/dashboard`) | Logged in, seeded data |
| `products.png` | Products (`/dashboard/products`) | Product table with a few rows |
| `product-forecast.png` | Product detail (`/dashboard/products/1`) | Scrolled to the AI Demand Forecast chart |
| `inventory.png` | Inventory (`/dashboard/inventory`) | AI Reorder Recommendations panel visible with at least one recommendation |
| `orders.png` | Orders (`/dashboard/orders`) | Order table |
| `order-detail.png` | Order detail (`/dashboard/orders/1`) | Status dropdown + line items visible |
| `customers.png` | Customers (`/dashboard/customers`) | Customer table |
| `reports.png` | Reports (`/dashboard/reports`) | All four export cards |
| `ai-assistant.png` | AI Assistant (`/dashboard/ai-assistant`) | Mid-conversation, at least one exchange visible |
| `dark-mode.png` | Any dashboard page | Dark mode toggled on |

Recommended capture width: ~1280px. PNG or JPG both work.
